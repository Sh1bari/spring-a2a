<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.sh1bari</groupId>
        <artifactId>a2a-spring-boot-server</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>a2a-spring-boot-server-jsonrpc-parent</artifactId>
    <packaging>pom</packaging>

    <name>A2A Spring Boot Server JSON-RPC Aggregator</name>
    <description>JSON-RPC Spring Boot server integration aggregator</description>

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>
</project>
